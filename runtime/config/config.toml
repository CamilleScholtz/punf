# The host you want to upload to.
# Possible values are "punpun.xyz" and "sr.ht".
host = "punpun.xyz"

# Your authentication key.
key = "hunter2"

# Your preferred screenshot command.
# Make sure to save the file to /tmp/screenshot.png.
scrot = "maim -o /tmp/screenshot.png"

# Your preferred selection screenshot command.
# Make sure to save the file to /tmp/screenshot.png.
selscrot = "maim -b 1 -c 0.921,0.376,0.517,1 -s -o /tmp/screenshot.png"

# Copy URL to clipboard.
clipboard = true

# Log URL to file.
log = true

# Display notification.
notification = false

# Print URL to stdout.
print = true

#
